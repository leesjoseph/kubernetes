apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vpro-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: profile.s-lee.it.kr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vproapp01
            port:
              number: 8080
 
